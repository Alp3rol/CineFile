/// Non-web stub — see `firebase_web_registrar.dart` for why this exists.
/// Native builds register Firebase plugins through the normal platform channel
/// mechanism, so there is nothing to do here.
void registerFirebaseWebPlugins() {}
