# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# AI Configuration
.claude/
CLAUDE.md


# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Private TMDb API Key configuration.
# Preferred way to supply a key is --dart-define=TMDB_API_KEY=... (see
# ApiConstants.tmdbApiKey); this file is the local-development fallback.
lib/core/constants/api_key.dart

# Downloaded npm dependencies, anywhere in the repo — the rules test suite
# (test_rules/) and the TMDb proxy (tools/tmdb-proxy/) both have some. Matched
# by name rather than by path so a third one added later is covered too; the
# path-specific rule this replaces silently missed tools/tmdb-proxy.
#
# The lockfiles ARE committed — they pin exact versions, and CI installs from
# them with `npm ci`.
node_modules/
# Emulator artefacts, written next to firebase.json when the suite runs.
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/

# Report written by `flutter gen-l10n` listing keys present in the template
# .arb but missing from a translation. Regenerated on every build and checked
# by CI — never committed. The generated app_localizations*.dart files next to
# the .arb files ARE committed, like the other generated code in this repo.
l10n_missing.json

# Raw app-icon/splash design source dump (see assets/icon/ for the
# actually-used, already-cropped derived assets committed to the repo).
/CineFileLOGO/
